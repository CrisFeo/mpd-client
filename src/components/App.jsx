'use strict';

const React = require('react');


(<style>
.app
  background-color: blue
</style>);

const App = props => (
  <div classNames='app'>
    <span>sup!</span>
  </div>
);

App.propTypes = {
};

module.exports = App;
